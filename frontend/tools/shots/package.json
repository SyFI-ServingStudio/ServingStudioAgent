{
  "name": "vibesim-ui-shots",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Headless-browser screenshots of the running VibeSim chat UI (dev utility, kept out of the frontend app's deps).",
  "scripts": {
    "browser": "playwright install chromium",
    "shoot": "node shoot.mjs"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
