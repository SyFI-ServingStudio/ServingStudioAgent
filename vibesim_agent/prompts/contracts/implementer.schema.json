{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ImplementerResult",
  "type": "object",
  "additionalProperties": false,
  "required": ["action", "message"],
  "properties": {
    "action": {
      "type": "string",
      "enum": ["progress", "milestone", "final_answer", "reply_user"]
    },
    "message": { "type": "string" }
  }
}
